platform: linux

image_resource:
  type: docker-image
  source:
    repository: engineerbetter/pcf-ops

inputs:
- name: gcp-bootstrap
outputs:
- name: tfstate-out

run:
  path: gcp-bootstrap/tasks/bootstrap.sh

params:
  GCP_CREDS_JSON:
  PROJECT_ID:

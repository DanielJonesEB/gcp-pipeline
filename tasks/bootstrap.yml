platform: linux

image_resource:
  type: docker-image
  source:
    repository: engineerbetter/pcf-ops

inputs:
- name: gcp-bootstrap
outputs:
- name: tfstate-out

run:
  path: gcp-bootstrap/tasks/bootstrap.sh

params:
  TF_VAR_gcp_creds:
  TF_VAR_project_id:
  PROJECT_ID:
